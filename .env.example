# Example environment variables for local execution and future automation.
# Replace placeholders with real values during implementation.

AWS_PROFILE=example-profile
AWS_REGION=us-east-1
GOOGLE_CLOUD_PROJECT=example-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/credentials.json
TF_VAR_environment=dev
TF_VAR_project_name=multicloud-waf-platform
