# Architecture

This directory contains the portfolio-level architecture source and rendered outputs for the multi-cloud WAF evaluation platform.

- `architecture.png` is the placeholder rendered architecture image
- `architecture.drawio` is the editable source placeholder for future diagram work
